                                    .....enter the element in 2d array by using user input....

         input :-

#include <stdio.h>

int main() {
    int marks[][3] = {{2,2,3},{3,5,3}};
        for(int i=0; i<2; i++)
    {
        for(int j=0; j<3; j++)
        {
            printf("the elemant of array %d,%d :-",i,j);
            scanf("%d",&marks[i][j]);
        }
    }    
    for(int i=0; i<2; i++)
    {
        for(int j=0; j<3; j++)
        {
            printf("\nthe elemant of array %d,%d is :- %d",i+1,j+1,marks[i][j]);
        }
        printf("\n");
    }
    return 0;
}

         output:-

the elemant of array 0,0 :-23
the elemant of array 0,1 :-32
the elemant of array 0,2 :-21
the elemant of array 1,0 :-12
the elemant of array 1,1 :-21
the elemant of array 1,2 :-32

the elemant of array 1,1 is :- 23
the elemant of array 1,2 is :- 32
the elemant of array 1,3 is :- 21

the elemant of array 2,1 is :- 12
the elemant of array 2,2 is :- 21
the elemant of array 2,3 is :- 32


=== Code Execution Successful ===
